# Copy to .env before running.
DATABASE_URL="file:./dev.db"     # SQLite (default). For Postgres see docker-compose.yml
JWT_SECRET="demo-secret-change-me"
JWT_EXPIRY="8h"
PORT=4000
NODE_ENV=development
